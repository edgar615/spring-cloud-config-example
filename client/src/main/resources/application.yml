management:
  info:
    defaults:
      enabled: true
  endpoints:
    web:
      exposure:
        include: refresh

spring:
#  profiles:
#    active: dev
  application:
    name: user
server:
  port: 9000


